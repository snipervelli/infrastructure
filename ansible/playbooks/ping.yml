---
- name: Verify Ansible connectivity
  hosts: rocky
  gather_facts: true

  tasks:
    - name: Ping managed hosts
      ansible.builtin.ping:

    - name: Show hostname
      ansible.builtin.debug:
        var: ansible_fqdn
